# AONP Integration Complete

The backend and adapter layer are consolidated under `aonp/` and validated via the tests in `tests/`.

## Quick Start

```bash
python aonp/api/main_v2.py
```

## Tests

```bash
python -m pytest tests/test_adapter_e2e.py
python -m pytest tests/test_integration_complete.py
```
